<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Habits</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="/css/UserDashboard.css" />
</head>

    <!-- Blue Header -->
    <header class="header bg-teal text-white py-3" style="background-color: #f4dfcc94;">
        <div class="container d-flex justify-content-between align-items-center">
            <!-- Profile Section Left -->
            <div class="d-flex align-items-center">
                <a href="/profile/<%= user.user_id %>">
                    <% if (user.profile_picture) { %>
                <img src="<%=user.profile_picture%>" alt="Profile" class="profile-img me-3" style="width: 60px; height: 60px; object-fit: cover;">
                <% } else { %>
                    <img src="/images/default.jpg" alt="Default Profile Picture" style="width: 60px; height: 60px; object-fit: cover;">
                <% } %>
                </a>
                <span style="margin-left: 20px; font-weight:bold">
                    Hello, <%= user.username%>
                    <i class="fa fa-cloud"></i>
                </span>
                
            </div>
            <div class="streak d-flex align-items-center mx-4">
                <i class="fas fa-fire text-warning" style="font-size: 1.5rem; margin-left: 300px; margin-right: 20px; margin-top:27px"></i> <!-- Added margin-right -->
                <span class="streak-count" style="font-size: 1.2rem; margin-top:27px;">
                    Streak: <%=streak.streak_count %> 
                </span>
            </div>
            
            
            
            <!-- Centered Search Bar -->
            <form method="GET" action="/api/users/search">
                <input type="text" class="form-control search-bar mx-auto" name="q" placeholder="Search for friends..." style="max-width: 400px;">
            </form>
            
    
            <!-- Friends and Logout Buttons Right -->
            <div class="d-flex top-right-buttons">
                <a href="/friends" class="btn btn-light btn-sm me-2">Friends</a>
                <a href="/logout" class="btn btn-light btn-sm me-2">Logout</a>
            </div>
        </div>
    </header>